<?xml version="1.0" encoding="utf-8"?>
<!-- Grok-style model chip: text + chevron only (no leading source glyph) -->
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">
    <solid android:color="@color/xai_canvas_soft" />
    <stroke android:width="1dp" android:color="@color/xai_border_outline" />
    <corners android:radius="999dp" />
    <padding
        android:left="4dp"
        android:top="4dp"
        android:right="2dp"
        android:bottom="4dp" />
</shape>
